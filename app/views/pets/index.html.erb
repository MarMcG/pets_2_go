<h1>Pets#index</h1>
<p>Find me in app/views/pets/index.html.erb</p>

<div class="cards">
  <% @pets.each do |pet|%>
    <div class="carde">
      <%= image_tag "pet_1.jpg" %>
      <div class="card-info">
        <div class="card-title"><%= pet.title %></div>
        <br>
        <div class="card-name">My name is <%= pet.name %>!</div>
        <div class="card-type">I'm a a <%= pet.pet_type %></div>
        <div class="card-age">I'm <%= pet.age %> years old</div>
        <div class="card-desc"><%= pet.descrption %></div>
        <div class="card-rate"><%= pet.rate %>$/day</div>
      </div>
    </div>
  <% end %>
</div>

      <%# <%= link_to 'Show', pet %>
      <%#<%= link_to "destroy_pet", pet_path(pet), method: :delete, data: { confirm: "You sure?" } %>
